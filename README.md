# SYSC4001 Assignment 2 – Part III  
**Process Fork/Exec API Simulator** 

## Overview
This project implements a simulator for `fork()` and `exec()` system calls in a simple single-CPU operating system. 

The simulator is written in **C++17** and tested under **Ubuntu 22.04**.

## Build & Run Instructions

### 1.Compile and run all tests

chmod +x build.sh
./build.sh

